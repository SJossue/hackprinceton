# FastAPI server + LLM query layer deps (Jossue)
# Install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# API framework
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# HTTP client (for K2 calls + misc)
httpx>=0.27

# LLM clients
anthropic>=0.40

# Env loading
python-dotenv>=1.0

# ElevenLabs is called via plain httpx (already a dep above) — no SDK needed.
# POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}
# Model: eleven_flash_v2_5 (low-latency, ~200ms first byte).
# If ELEVENLABS_API_KEY or ELEVENLABS_VOICE_ID unset → TTS returns None and
# answers are delivered text-only (no audio in the /ws/state broadcast).
